{
    "require": {
        "tightenco/jigsaw": "^1.8",
        "samdark/sitemap": "^2.3.0",
        "symfony/process": "^7.3"
    },
    "autoload": {
      "psr-4": {
        "App\\Listeners\\": "listeners/"
      }
    },
    "prefer-stable": true,
    "scripts": {
        "build:prod": "npm run build && vendor/bin/jigsaw build prod",
        "build:local": "npm run build && vendor/bin/jigsaw build",
        "watch": "npm run dev && vendor/bin/jigsaw serve"
    }
}
